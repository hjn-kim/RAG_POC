# 로컬 실행용 (app-local.py / export_results.py). 실제 추출을 돌리는 쪽.
#   pip install -r requirements-local.txt
streamlit>=1.54
pandas>=2.0
docling>=2.0
pymupdf>=1.24
python-docx>=1.1
pdfplumber>=0.11
mammoth>=1.8
unstructured[docx,pdf]>=0.25
# unstructured[pdf] extra 가 끌어오지 않지만 partition/pdf.py 가 모듈 최상단에서
# 임포트한다. strategy="fast" 에도 필요하므로 명시한다.
unstructured-inference>=1.6
