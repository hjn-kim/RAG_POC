# 배포(app.py) 전용. app.py 는 results.json 을 읽어 그리기만 하므로 추출 라이브러리가
# 필요 없다. Docling·unstructured 를 여기 넣으면 Streamlit Community Cloud 에서
# 설치·모델 로딩만으로 리소스 한도를 넘겨 앱이 죽는다.
#
# 로컬에서 실제 추출을 돌릴 때는 requirements-local.txt 를 쓴다.
streamlit>=1.54
